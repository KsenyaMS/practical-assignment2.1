import { createTheme } from '@mui/material';

export const MainTheme = createTheme({
    palette: {
        background: {
            default: '#FAFBFB',
        },
    },
    typography: {
        fontSize: 14,
    },
});